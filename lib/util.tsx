// Re-export from the new PDF utils for backward compatibility
export { getResumeImage } from './pdf-utils';

// Default export for backward compatibility
import { getResumeImage } from './pdf-utils';
export default getResumeImage;